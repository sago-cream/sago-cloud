#!/usr/bin/env bash
set -euo pipefail

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)/lib/common.sh"

for stack in "${STACKS[@]}"; do
  printf '\n[%s]\n' "$stack"
  compose "$stack" ps
done

printf '\n'
"$OPERATIONS_ROOT/scripts/sago-media-status"
